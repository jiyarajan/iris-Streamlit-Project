# Iris Flower Prediction using SVM

This project predicts the species of an Iris flower using a Support Vector Machine (SVM) model.

## Technologies Used
- Python
- Streamlit
- Scikit-learn
- NumPy
- Joblib

## Run the Project

Install dependencies:

pip install -r requirements.txt

Run the application:

streamlit run iris.py